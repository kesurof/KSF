services:
  wp:
    image: wordpress:7-fpm-alpine
    container_name: ${APP_INSTANCE}-wp
    user: "${APP_PUID}:${APP_PGID}"
    restart: unless-stopped
    networks:
      - proxy
    depends_on:
      db:
        condition: service_healthy
      cache:
        condition: service_started
    env_file:
      # Generated per instance by pre_install.sh with mode 600.
      - ./.env
    environment:
      TZ: ${TZ_VALUE}
      WORDPRESS_DEBUG: "1"
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_CONFIG_EXTRA: |
        // Object cache Redis
        define('WP_REDIS_HOST', 'cache');
        define('WP_REDIS_PORT', 6379);
        define('WP_CACHE_KEY_SALT', getenv('WORDPRESS_CACHE_KEY_SALT'));
        define('WP_CACHE', true);
        // Keys and salts are injected from the instance-only env_file.
        define('AUTH_KEY', getenv('WORDPRESS_AUTH_KEY'));
        define('SECURE_AUTH_KEY', getenv('WORDPRESS_SECURE_AUTH_KEY'));
        define('LOGGED_IN_KEY', getenv('WORDPRESS_LOGGED_IN_KEY'));
        define('NONCE_KEY', getenv('WORDPRESS_NONCE_KEY'));
        define('AUTH_SALT', getenv('WORDPRESS_AUTH_SALT'));
        define('SECURE_AUTH_SALT', getenv('WORDPRESS_SECURE_AUTH_SALT'));
        define('LOGGED_IN_SALT', getenv('WORDPRESS_LOGGED_IN_SALT'));
        define('NONCE_SALT', getenv('WORDPRESS_NONCE_SALT'));
        // Durcissement
        define('DISALLOW_FILE_EDIT', true);
        define('FORCE_SSL_ADMIN', true);
        define('AUTOMATIC_UPDATER_DISABLED', false);
        define('WP_AUTO_UPDATE_CORE', 'minor');
        // Memoire
        define('WP_MEMORY_LIMIT', '256M');
        define('WP_MAX_MEMORY_LIMIT', '512M');
        // Post-revisions limitees (perf DB)
        define('WP_POST_REVISIONS', 10);
        // Intervalle autosave
        define('AUTOSAVE_INTERVAL', 120);
        // Vide auto de la corbeille
        define('EMPTY_TRASH_DAYS', 14);
    volumes:
      - ${BASE_DIR}/data/${APP_INSTANCE}/html:/var/www/html
      - ${BASE_DIR}/data/${APP_INSTANCE}/config/php.ini:/usr/local/etc/php/conf.d/99-wordpress.ini:ro
      - ${BASE_DIR}/data/${APP_INSTANCE}/config/opcache.ini:/usr/local/etc/php/conf.d/zz-opcache.ini:ro
    healthcheck:
      test: ["CMD-SHELL", "php -r 'exit(is_dir(\"/var/www/html/wp-admin\") ? 0 : 1);'"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  web:
    image: nginx:1.27-alpine
    container_name: ${APP_INSTANCE}-web
    restart: unless-stopped
    networks:
      - proxy
    ${APP_PORTS_BLOCK}
    depends_on:
      wp:
        condition: service_started
    environment:
      - PUID=${APP_PUID}
      - PGID=${APP_PGID}
      - TZ=${TZ_VALUE}
    volumes:
      - ${BASE_DIR}/data/${APP_INSTANCE}/html:/var/www/html:ro
      - ${BASE_DIR}/data/${APP_INSTANCE}/config/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://127.0.0.1/ >/dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

  db:
    image: mariadb:11
    container_name: ${APP_INSTANCE}-db
    restart: unless-stopped
    networks:
      - proxy
    env_file:
      # Generated per instance by pre_install.sh with mode 600.
      - ./.env
    environment:
      - TZ=${TZ_VALUE}
      - MARIADB_RANDOM_ROOT_PASSWORD=true
    volumes:
      - ${BASE_DIR}/data/${APP_INSTANCE}/db:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --innodb-buffer-pool-size=256M
      - --innodb-log-file-size=64M
      - --max-connections=200
      - --max-allowed-packet=128M
      - --query-cache-size=0
      - --query-cache-type=0

  cache:
    image: redis:7-alpine
    container_name: ${APP_INSTANCE}-cache
    restart: unless-stopped
    networks:
      - proxy
    environment:
      - PUID=${APP_PUID}
      - PGID=${APP_PGID}
      - TZ=${TZ_VALUE}
    volumes:
      - ${BASE_DIR}/data/${APP_INSTANCE}/cache:/data
    command:
      - redis-server
      - --maxmemory
      - 128mb
      - --maxmemory-policy
      - allkeys-lru
      - --save
      - "300 100"
      - --appendonly
      - "no"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 5s
      retries: 3

networks:
  proxy:
    name: ${NETWORK_NAME}
    external: true
